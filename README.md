# ServerDebugNetwork

To run the example project, clone the repo, and run `pod install` from the Example directory first.

## Requirements

### Install NodeJS

```shell
brew install node
npm install
```

### Run server
cd to root folder
```
./run-server-debug.sh
```
